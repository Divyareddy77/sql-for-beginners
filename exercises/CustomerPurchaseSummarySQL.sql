-- Write a SQL query below --
SELECT id as order_id, ROUND(total_amount,2) AS total_amount, TO_CHAR(ordered_at,'YYYY-MM-DD HH24:MI:SS') AS ordered_at FROM orders WHERE order_status='Delivered' AND total_amount>150.00 ORDER BY ordered_at DESC;